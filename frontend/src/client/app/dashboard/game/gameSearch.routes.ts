import { Route } from '@angular/router';

import { GameSearchComponent } from './gameSearch.component';

export const GameSearchRoutes: Route[] = [
  {
    path: 'search',
    component: GameSearchComponent
  }
];
